// Get Schema and Types from Mongoose
const { Schema, Types } = require('mongoose');

// Create a friendSchema
const friendSchema = new Schema();

// Export the friendSchema
module.exports = friendSchema;
